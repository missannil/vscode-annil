import { CustomComponent } from "annil";
import type { $SubExternal } from "~/subExternal/subExternal.js";
import type { Root } from "./externalImports.js";

export const cart = CustomComponent<Root, $SubExternal>()({});
