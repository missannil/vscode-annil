<!-- ① wx:for 作用域内，item 可作为自定义组件的传值 → 无诊断 -->
<block
	wx:for="{{list}}"
	wx:for-item="item"
	wx:for-index="index"
>
	<subInline customValue="{{item}}"></subInline>
	<!-- annil disable repeatTag -->
	<!-- ② 缺少自定义属性 → 缺少属性诊断 -->
	<subInline></subInline>
</block>
