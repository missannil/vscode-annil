// @ts-nocheck

const subA = SubComponent()({
  events: {
    subA_aaa() {},
  },
});

const subB = SubComponent()({
  events: {
    subB_bbb() {},
  },
});

const subC = SubComponent()({
  events: {
    subC_ccc() {},
  },
});
